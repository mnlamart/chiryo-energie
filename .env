# Server-side (used by loaders, actions)
BASE_URL=http://localhost:5173

# Client-side (exposed by Vite, must have VITE_ prefix)
VITE_BASE_URL=http://localhost:5173

# Indexing control
ALLOW_INDEXING=false
